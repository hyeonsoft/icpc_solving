#include <iostream>
#include<algorithm>

int nsize;
int n[1000000];
int msize;
int m[1000000];
using namespace std;

bool int find(int *n, int nsize) {

}

int main() {
	cin >> nsize;
	for (int i = 0; i < nsize; i++) {
		cin >> n[i];
	}
	sort(n, n + nsize);
	
}